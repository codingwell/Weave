apply plugin: 'scala'

dependencies {
   compile libraries.scala_guice
   compile libraries.guava
   compile libraries.guice
   compile libraries.guice_multibindings
   compile libraries.commons_io
   compile libraries.akka_actor
}

// vim: syntax=groovy
